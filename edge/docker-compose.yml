services:
  ast-service:
    build: ./ast-service
    devices:
      - /dev/snd:/dev/snd
    privileged: true
    environment:
      - PYTHONUNBUFFERED=1
      - DB_HOST=db
      - DB_NAME=soundscape
      - DB_USER=postgres
      - DB_PASSWORD=changeme
      - DEVICE_NAME=AudioMoth
      - SAMPLE_RATE=192000
    volumes:
      - audio_lock:/locks
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  batdetect-service:
    build:
      context: ..
      dockerfile: edge/batdetect-service/Dockerfile
    devices:
      - /dev/snd:/dev/snd
    privileged: true
    environment:
      - PYTHONUNBUFFERED=1
      - DB_HOST=db
      - DB_NAME=soundscape
      - DB_USER=postgres
      - DB_PASSWORD=changeme
      - DEVICE_NAME=AudioMoth
      - SAMPLE_RATE=192000
      - DETECTION_THRESHOLD=0.3
      - UPLOAD_BAT_AUDIO=false
      - ENABLE_GROUPS_CLASSIFIER=${ENABLE_GROUPS_CLASSIFIER:-false}
      - MODEL_PATH=/app/models/groups_model.pt
      - MODEL_VERSION=${MODEL_VERSION:-groups_v1_post_epfu_partial_2026-04-17}
    volumes:
      - audio_lock:/locks
      - bat_audio:/bat_audio
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  sync-service:
    build: ./sync-service
    environment:
      - PYTHONUNBUFFERED=1
      - DB_HOST=db
      - DB_NAME=soundscape
      - DB_USER=postgres
      - DB_PASSWORD=changeme
      - SYNC_INTERVAL=60
      - SAMPLE_RATE=192000
      - FIREBASE_PROJECT_ID=${FIREBASE_PROJECT_ID}
      - FIREBASE_STORAGE_BUCKET=bat-edge-monitor.firebasestorage.app
      - UPLOAD_BAT_AUDIO=false
    volumes:
      - ./sync-service/serviceAccountKey.json:/app/serviceAccountKey.json:ro
      - /proc/uptime:/host/uptime:ro
      - /proc/meminfo:/host/meminfo:ro
      - /proc/loadavg:/host/loadavg:ro
      - /sys/class/thermal/thermal_zone0/temp:/host/cpu_temp:ro
      - bat_audio:/bat_audio
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  db:
    image: postgres:16
    environment:
      POSTGRES_DB: soundscape
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: changeme
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  hobo-ble-service:
    build: ./hobo-ble-service
    privileged: true
    network_mode: host
    volumes:
      - /var/run/dbus:/var/run/dbus
    environment:
      - PYTHONUNBUFFERED=1
      - DB_HOST=localhost
      - DB_NAME=soundscape
      - DB_USER=postgres
      - DB_PASSWORD=changeme
      - HOBO_MODEL=${HOBO_MODEL:-MX2201}
      - HOBO_POLL_INTERVAL=${HOBO_POLL_INTERVAL:-30}
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  analysis-api:
    build:
      context: ..
      dockerfile: edge/analysis-api/Dockerfile
    environment:
      - PYTHONUNBUFFERED=1
      - DB_HOST=db
      - DB_NAME=soundscape
      - DB_USER=postgres
      - DB_PASSWORD=changeme
      - DETECTION_THRESHOLD=0.3
      - ENABLE_GROUPS_CLASSIFIER=${ENABLE_GROUPS_CLASSIFIER:-false}
      - MODEL_PATH=/app/models/groups_model.pt
      - MODEL_VERSION=${MODEL_VERSION:-groups_v1_post_epfu_partial_2026-04-17}
    ports:
      - "8080:8080"
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

volumes:
  pgdata:
  audio_lock:
  bat_audio:
